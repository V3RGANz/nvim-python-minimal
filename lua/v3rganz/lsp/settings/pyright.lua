return {
    settings = {
        -- TODO: Need ot somehow disable it
        -- filetypes = {
        --     exclude = {
        --         "**/venv/**",
        --         "**/bazel-build/**",
        --     }
        -- }
    }
}
